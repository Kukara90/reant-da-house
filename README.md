### Build rent da house

`cd rentDaHouse`

`mvn clean install`

### Run via docker-compose

`cd rentDaHouse`

`docker build -t rdh-app-backend .`

`docker-compose up`

### Swagger UI

`[host]/rent-da-house-api/swagger`