spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/rent_da_house
    username: postgres
    password: postgres
  jpa:
    show-sql: true

logging:
  level:
    org:
      springframework:
        data:
          jpa: DEBUG